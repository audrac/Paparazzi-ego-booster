<div class="container">
  <h1>Bookings:</h1>
    <% @bookings.each do |booking|%>
      <li><%= booking.paparazzi_id %></li>
      <li><%= booking.booking_location %></li>
      <li><%= booking.meeting_date %></li>
      <li><%= booking.special_request %></li>
    <% end %>
    <%= link_to "Home", root_path %>
</div>
